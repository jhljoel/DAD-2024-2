server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jhljoel/DAD-2024-2.git
          searchPaths: config-data
          #username:
          #password:
          default-label: config-server
  security:
    user:
      name: root
      password: